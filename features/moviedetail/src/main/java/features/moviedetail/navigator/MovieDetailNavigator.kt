package features.moviedetail.navigator

import androidx.appcompat.app.AppCompatActivity

class MovieDetailNavigator(private val activity: AppCompatActivity) {

    fun onUnkownError() {
        // TODO: show simple dialog
    }

}